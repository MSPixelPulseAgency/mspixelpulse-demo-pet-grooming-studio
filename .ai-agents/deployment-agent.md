# Deployment Agent

Deploy this project as `mspixelpulse-demo-pet-grooming-studio`.

- Framework: Vite.
- Install command: `npm install`.
- Build command: `npm run build`.
- Output directory: `dist`.
- Root directory: `./`.
- Confirm the repo is `MSPixelPulseAgency/mspixelpulse-demo-pet-grooming-studio`.
- Production deploy only after install, build, reference search, commit, and push are complete.
